# Firefight AI 触控方式 — 验证通过版本

## 📌 版本锁定
- **Commit**: `027dfc9`
- **日期**: 2026-07-19 14:48
- **文件**: `src/controller/game_controller.py`
- **验证结果**: 放大42.91% / 缩小38.05% / 标记变化确认真缩放 / 无抖动

---

## 🔍 缩放方法 `_zoom`

```python
def _zoom(self, direction: str = "in", cx: int = 540, cy: int = 960) -> None:
    """缩放: Win32 keybd_event(Ctrl) + mouse_event(Wheel) → MuMu窗口

    唯一有效方案: 直发 Win32 键盘+滚轮事件到 MuMu 窗口,
    标记变化验证: 蓝74→17(放大) →83(缩小), 确认真缩放!
    """
    import ctypes
    user32 = ctypes.windll.user32

    # 找MuMu窗口
    result = []
    def cb(h, _):
        buf = ctypes.create_unicode_buffer(256)
        user32.GetWindowTextW(h, buf, 256)
        if buf.value == "MuMu安卓设备":
            result.append(h)
        return True
    user32.EnumWindows(
        ctypes.WINFUNCTYPE(ctypes.c_bool, ctypes.c_int, ctypes.c_int)(cb), 0
    )
    hwnd = result[0] if result else None

    if not hwnd:
        logger.warning("找不到MuMu窗口, 缩放失败")
        return

    user32.SetForegroundWindow(hwnd)
    sign = 120 if direction == "in" else -120
    n = 10  # 10格缩放

    for _ in range(n):
        user32.keybd_event(0x11, 0, 0, 0)  # Ctrl down
        user32.mouse_event(0x0800, 0, 0, sign, 0)  # wheel
        user32.keybd_event(0x11, 0, 2, 0)  # Ctrl up
        time.sleep(0.01)
    time.sleep(0.2)
```

**技术细节**:
- `0x11` = VK_CTRL
- `0x0800` = MOUSEEVENTF_WHEEL
- `sign = 120` 放大, `-120` 缩小 (WHEEL_DELTA)
- `n=10` 每次10格
- `keybd_event(..., 0, 2, 0)` = `KEYEVENTF_KEYUP`
- `SetForegroundWindow` 确保事件送到MuMu

---

## 🎯 执行指令 `_fast_execute`

```python
def _fast_execute(self, commands: list[ParsedCommand], state: GameState) -> float:
    """执行指令: select=批量tap(快), move=双击中圈+swipe拖拽(已验证)

    select: 批量tap → 选中自动接敌 (<=300ms)
    move:   单单位 → tap选中→双击中圈→swipe拖到目标 (~3.5s)
            验证: 双击后swipe=拖动goto bar, 非镜头平移 (2026-07-19)
    """
```

**执行流程**:

### ① Select (批量tap)
```python
select_cmds: list[str] = []
for cmd in commands:
    if cmd.action.value in ("select",):
        for uid in cmd.unit_ids[:8]:
            unit = state.get_unit_by_id(uid)
            if unit:
                select_cmds.append(f"input tap {unit.x} {unit.y}")

if select_cmds:
    batch = "; ".join(select_cmds)
    subprocess.run(
        [self.MUMU_EXE, "control", "-v", "0", "tool", "cmd", "-c", batch],
        ...
    )
```

### ② 双指缩放
```python
for cmd in commands:
    if cmd.action.value in ("zoom_in", "zoom_out"):
        direction = "in" if cmd.action.value == "zoom_in" else "out"
        self._zoom(direction, cx, cy)
```

### ③ Move (双击中圈 + swipe拖拽)
```python
for cmd in commands:
    if cmd.action.value in ("move", "attack"):
        unit = state.get_unit_by_id(cmd.unit_ids[0])
        ux, uy = unit.x, unit.y
        tx, ty = cmd.target_pixel
        cx, cy = ux, uy  # 中圈≈单位中心

        # 1. tap 选中单位
        self._mumu_cmd(f"input tap {ux} {uy}")
        time.sleep(0.6)

        # 2. 双击中圈 (抓取蓝条)
        self._mumu_cmd(f"input tap {cx} {cy}")
        time.sleep(0.05)
        self._mumu_cmd(f"input tap {cx} {cy}")
        time.sleep(0.1)

        # 3. swipe 拖拽蓝条到目标
        self._mumu_cmd(f"input swipe {cx} {cy} {tx} {ty} 2000")
        time.sleep(2.3)
```

---

## 📷 截图 `_fast_capture`

```python
def _fast_capture(self) -> tuple[np.ndarray | None, float]:
    """ADB screencap - 直接内存解码, 不落地磁盘"""
    result = subprocess.run(
        [self.ADB_EXE, "-s", f"127.0.0.1:{self.ADB_PORT}", "exec-out", "screencap", "-p"],
        capture_output=True, timeout=8,
    )
    img_array = np.frombuffer(result.stdout, dtype=np.uint8)
    frame = cv2.imdecode(img_array, cv2.IMREAD_COLOR)
    return frame, elapsed
```

---

## 🎨 颜色检测 `_fast_detect`

```python
def _fast_detect(self, frame: np.ndarray):
    """纯HSV颜色检测 + 距离聚类 → 友军/敌军坐标"""
    small = cv2.resize(frame, (w // 2, h // 2))
    hsv = cv2.cvtColor(small, cv2.COLOR_BGR2HSV)

    # 友军: HSV(95-115, 100-255, 100-255) 蓝色
    ally_mask = cv2.inRange(hsv, ALLY_HSV_LOW, ALLY_HSV_HIGH)

    # 敌军: HSV(0-10) 和 (170-180) 两段红色
    enemy_mask = cv2.bitwise_or(enemy_1, enemy_2)

    # connectedComponentsWithStats + 距离聚类(eps=50px)
```

**聚类逻辑**: 同一班组3-8个士兵间距<50px → 合并为一个可指挥单位，取离中心最近的蓝点作为点击数保证命中。

---

## 🔧 MuMu 通信

```python
def _mumu_cmd(self, cmd: str) -> None:
    subprocess.run([
        "D:\\MuMuPlayer\\nx_main\\MuMuManager.exe",
        "control", "-v", "0", "tool", "cmd", "-c", cmd
    ], capture_output=True, text=True, timeout=5)
```

---

## ⚙️ 关键配置

| 参数 | 值 | 说明 |
|------|-----|------|
| ADB地址 | `127.0.0.1:7555` | MuMu模拟器ADB |
| 聚类距离 | 50px | 同班组士兵间距 |
| 选中延迟 | 0.6s | tap后等待注册 |
| 拖拽时长 | 2000ms | swipe duration |
| 友军HSV | [95,100,100]-[115,255,255] | 蓝标记 |
| 敌军HSV | [0,100,100]-[10,255,255] 和 [170,100,100]-[180,255,255] | 红标记 |
| 颜色阈值面积 | ≥10 (缩略图) | 过滤噪点 |
